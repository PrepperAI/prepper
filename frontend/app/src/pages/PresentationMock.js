import AudioTest from "../components/AudioComponent";
import PresentationMode from "../components/Presentation/PresentationMode";

export default function PresentationMock() {
  return (
    <div style={{ padding: "2rem" }}>
      <PresentationMode />
    </div>
  );
}
